% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTD.R
\name{MTD-database}
\alias{MTD-database}
\alias{setMtdDatabase}
\alias{getMtdDatabase}
\title{Add or Update Database Metadata in an MTD section}
\usage{
setMtdDatabase(
  x = matrix(),
  name = character(),
  prefix = character(),
  version = character(),
  uri = character(),
  replace = FALSE
)

getMtdDatabase(x = matrix())
}
\arguments{
\item{x}{A MTD section that stores metadata fields. Defaults to \code{matrix()}.}

\item{name}{\code{character} with the description of databases used. For cases,
where a known database has not been used for identification.
(e.g., \code{"[MIRIAM, MIR:00100079, HMDB, ]"}).}

\item{prefix}{\code{character} with the prefix used in the “identifier” column of
data tables. (e.g., \code{"hmdb"}).}

\item{version}{\code{character} with the database version is mandatory where
identification has been performed. This may be a formal version number
(\code{"1.4.1"}), a date of access (\code{"2016-10-27"}) (ISO-8601 format) or
(\code{"Unknown"}) if there is no suitable version that can be annotated.}

\item{uri}{\code{character} with the URI to the database.}

\item{replace}{\code{logical} flag controlling how pre-existing database
metadata is handled:
\itemize{
\item \code{FALSE} (default): new values are appended to any existing
values.
\item \code{TRUE}: existing instrument metadata is discarded and
replaced entirely by the supplied arguments.
}}
}
\value{
The input object \code{x} updated to include the new or merged database
metadata fields. If \code{x} is empty, the empty \code{x}.

\code{character} with the database.
}
\description{
Sets or updates database-related metadata fields within an MTD (metadata)
section. When database metadata already exists, the function can either
replace it entirely or append new values to the existing ones.

Get the databse fields within an MTD (metadata) section.
}
\examples{

x <- mtd_skeleton("001", software = "[MS, MS:1001582, xmcs, 4.0.0]")
## Add database metadata to an existing mzTab object
mtd <- setMtdDatabase(x, name = "[MIRIAM, MIR:00100079, HMDB, ]",
          prefix = "hmdb",
          version = "3.6",
          uri = "http://www.hmdb.ca/")

## Replace all existing database metadata
mtd <- setMtdDatabase(mtd, name = "[MIRIAM, MIR:00100079, HMDB, ]",
          prefix = "hmdb",
          version = "3.6",
          uri = "http://www.hmdb.ca/",
          replace = TRUE)


x <- mtd_skeleton("001", software = "[MS, MS:1001582, xcms, 4.0.0]")
mtd <- setMtdDatabase(x, name = "[MIRIAM, MIR:00100079, HMDB, ]",
          prefix = "hmdb",
          version = "3.6",
          uri = "http://www.hmdb.ca/")

getMtdDatabase(x)

}
\author{
Gabriele Tomè
}
