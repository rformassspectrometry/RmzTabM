% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTD.R
\name{MTD-instrument}
\alias{MTD-instrument}
\alias{setMtdInstrument}
\alias{getMtdInstrument}
\title{Add or Update Instrument Metadata in an MTD section}
\usage{
setMtdInstrument(
  x = matrix(),
  name = character(),
  source = character(),
  analyzer = character(),
  detector = character(),
  replace = FALSE
)

getMtdInstrument(x = matrix())
}
\arguments{
\item{x}{A MTD section that stores metadata fields. Defaults to \code{matrix()}.}

\item{name}{\code{character} with the name of the instrument used in the
experiment. (e.g., \code{"[MS, MS:1000449, LTQ Orbitrap,]"}).}

\item{source}{\code{character} with the instrument’s source used in the
experiment. (e.g., \code{"[MS, MS:1000073, ESI,]"}).}

\item{analyzer}{\code{character} with the instrument’s analyzer type(s) used in
the experiment.
(e.g., \code{c("analyzer[1]" = "[MS, MS:1000291, linear ion trap,]")}).}

\item{detector}{\code{character} with the instrument’s detector type used in the
experiment. (e.g., \code{"[MS, MS:1000253, electron multiplier,]"}).}

\item{replace}{\code{logical} flag controlling how pre-existing instrument
metadata is handled:
\itemize{
\item \code{FALSE} (default): new values are appended to any existing
values.
\item \code{TRUE}: existing instrument metadata is discarded and
replaced entirely by the supplied arguments.
}}
}
\value{
The input object \code{x} updated to include the new or merged instrument
metadata fields. If \code{x} is empty, the empty \code{x}.

\code{character} with the Instrument.
}
\description{
Sets or updates instrument-related metadata fields within an MTD (metadata)
section. When instrument metadata already exists, the function can either
replace it entirely or append new values to the existing ones.

Get the Instrument fields within an MTD (metadata) section.
}
\examples{

x <- mtd_skeleton("001", software = "[MS, MS:1001582, xmcs, 4.0.0]")
## Add instrument metadata to an existing mzTab object
mtd <- setMtdInstrument(x, name = "[MS, MS:1000449, LTQ Orbitrap,]",
          source = "[MS, MS:1000073, ESI,]",
          analyzer = c(`analyzer[1]` = "[MS, MS:1000291, linear ion trap,]"),
          detector = "[MS, MS:1000253, electron multiplier,]")

## Replace all existing instrument metadata
mtd <- setMtdInstrument(mtd, name = "[MS, MS:1000449, LTQ Orbitrap,]",
          source = "[MS, MS:1000073, ESI,]",
          analyzer = c(`analyzer[1]` = "[MS, MS:1000291, linear ion trap,]"),
          detector = "[MS, MS:1000253, electron multiplier,]",
          replace = TRUE)


x <- mtd_skeleton("001", software = "[MS, MS:1001582, xcms, 4.0.0]")
mtd <- setMtdInstrument(x, name = "[MS, MS:1000449, LTQ Orbitrap,]",
          source = "[MS, MS:1000073, ESI,]",
          analyzer = c(`analyzer[1]` = "[MS, MS:1000291, linear ion trap,]"),
          detector = "[MS, MS:1000253, electron multiplier,]")

getMtdInstrument(x)

}
\author{
Gabriele Tomè
}
