% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions-utils.R
\name{parseCvParameter}
\alias{parseCvParameter}
\alias{isCvParameter}
\title{Utility functions for CV parameters}
\usage{
parseCvParameter(x, element = 2L)

isCvParameter(x)
}
\arguments{
\item{x}{\code{character} with the CV parameter(s) to parse or test.}

\item{element}{\code{integer(1)} defining which \emph{element} to extract: \code{1} for the
CV label, \code{2} for the CV term (accession), \code{3} for the name and \code{4} for
the value element.}
}
\value{
\code{character} of \code{length(x)} with the parsed CV parameter elements or
\code{NA_character_} if not present.
}
\description{
mzTab-M makes use of controlled vocabulary (CV) parameters. These parameters
are expected to be provided in the format
\code{"[CV label, accession, name, value]"}. In addition, also \emph{user parameters}
where only the \emph{name} and \emph{value} are provided are supported, but it is
recommendet to use full CV parameters where possible.

\code{parseCvParameter()} allows to extract individual fields from a CV
parameter.

\code{isCvParameter()} tests whether a string is in the expected (CV parameter)
format.
}
\note{
While mzTab supports \code{","} in the \emph{name} and \emph{value} field (in which case
the respective field must be placed between \verb{"}), this is currently not
supported. Thus, CV label and term (accession) are expected to be correctly
extracted, the name and value field might not if \code{","} are present in them.
}
\examples{

## Extract CV term
x <- c("[MS, MS:1002962, mean, ]", "[MS, MS:1002883, median, ]")
parseCvParameter(x, 1)

parseCvParameter(x)

parseCvParameter(x, 3)

parseCvParameter(x, 4)

## CV term missing
parseCvParameter("[, , user, value]")
parseCvParameter("[, , user, value]", 3)
parseCvParameter("[, , user, value]", 4)

## Check validity of CV parameters
isCvParameter(c(x, "[a, b, c, d, e]", "d"))
}
\author{
Johannes Rainer
}
\concept{mzTab-M utility functions}
