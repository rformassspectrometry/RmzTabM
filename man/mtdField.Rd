% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTD.R
\name{mtdField}
\alias{mtdField}
\title{Add or Update a Metadata Field in an MTD Object}
\usage{
mtdField(x = matrix(), field = character(), value = character(), add = TRUE)
}
\arguments{
\item{x}{A MTD object that stores metadata fields. Defaults to \code{matrix()}.
If all values are \code{NA}, the function returns \code{x} unchanged.}

\item{field}{\code{character} name of the metadata field to set or update.
Must be a valid \href{https://github.com/HUPO-PSI/mzTab-M/blob/main/specification_documents/mzTab_format_specification_2_1-M.adoc#62-metadata-section}{MTD field name}. (e.g. \code{"publication"})}

\item{value}{\code{character} value(s) to assign to the field. (e.g.
\code{"https://pubs.acs.org/doi/10.1021/acs.analchem.8b04310"})}

\item{add}{\code{logical} flag controlling how pre-existing field
metadata is handled:
\itemize{
\item \code{TRUE} (default): new values are appended to any existing
values.
\item \code{FALSE}: existing field metadata is discarded and
replaced entirely by the supplied arguments.
}}
}
\value{
The input object \code{x} updated to include the new or merged field
metadata. If \code{x} is empty, the empty \code{x}.
}
\description{
Sets or updates a generic metadata field within an MTD (metadata)
in a MTD object. When the field already exists, the function
can either replace it entirely or append new values to the existing ones.
}
\examples{

x <- mtd_skeleton("001", software = "[MS, MS:1001582, xcms, 4.0.0]")
## Add a metadata field to an existing mzTab object
mtd <- mtdField(x, field = "publication",
          value = "https://pubs.acs.org/doi/10.1021/acs.analchem.8b04310")

## Replace all existing values for a metadata field
mtd <- mtdField(mtd, field = "custom",
          value = "[,,MS operator, Florian]",
          add = FALSE)

}
\author{
Gabriele Tomè
}
