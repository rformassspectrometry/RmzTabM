% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTD.R
\name{setMtdProtocol}
\alias{setMtdProtocol}
\alias{setMtdProtocol,dfmatrix-method}
\alias{setMtdProtocol,MzTabM-method}
\alias{getMtdProtocol}
\title{Add or Update protocol Metadata  of an mzTab-M MTD section}
\usage{
\S4method{setMtdProtocol}{dfmatrix}(
  x = matrix(),
  name = character(),
  type = character(),
  description = character(),
  parameters = character(),
  replace = FALSE
)

getMtdProtocol(x = matrix())
}
\arguments{
\item{x}{A MTD section that stores metadata fields. Can be a two-column
\code{character} matrix, a two-column \code{data.frame} or a \code{\link[=MzTabM]{MzTabM()}} object.
Defaults to \code{matrix()}. If all values are \code{NA}, the function returns \code{x}
unchanged.}

\item{name}{\code{character} with protocol name describing one or more steps of
an experimental procedure, such as sample preparation, data acquisition
or data processing.}

\item{type}{\code{character} with the protocol type, as defined by the parameter.
Can be of length 1 or equal to \code{length(name)}.}

\item{description}{optional \code{character} with the description of the protocol.
Can be of length 1 or equal to \code{length(name)}.}

\item{parameters}{optional \code{character} with additional parameters of the
protocol}

\item{replace}{\code{logical} flag controlling how pre-existing contact
metadata is handled:
\itemize{
\item \code{FALSE} (default): new values are appended to any existing values.
\item \code{TRUE}: existing instrument metadata is discarded and replaced
entirely by the supplied arguments.
}}
}
\value{
\itemize{
\item For \code{setMtdProtocol()}: the input object \code{x} updated to include the new or
merged protocol metadata fields. If \code{x} is empty, the empty \code{x}.
\item For \code{getMtdProtocol()}: a named \code{character} with the protocol information,
names being the field names.
}
}
\description{
\code{setMtdProtocol()} sets or updates protocol-related metadata fields within
an MTD (metadata) section. When protocol metadata already exists, the
function can either replace it entirely or append new values to the existing
ones. Each protocol is referenced from an \emph{assay} section (see \code{\link[=mtdAssay]{mtdAssay()}}).

\code{getMtdProtocol()} returns the protocol information from an MTD section.
}
\examples{

x <- mtdSkeleton("001", software = "[MS, MS:1001582, xmcs, 4.0.0]")
## Minimal example with protocol.
mtd <- setMtdProtocol(x, name = c("Mass Spectrometry"),
       type = c("[CHMO, CHMO:0000470, mass spectrometry, ]"),
       description = c("Eluting compounds were detected ..."),
       parameters = paste0("[MS, MS:1000008, ionization type, ",
                           "[MS,MS:1000073, electrospray ionization, ]]"))

## Example with all the fields and replace the previous
mtd <- setMtdProtocol(mtd, name = c("Mass Spectrometry", "extraction"),
            type = c("[CHMO, CHMO:0000470, mass spectrometry, ]",
                     "[MSIO, MSIO:0000141, metabolite extraction,]"),
            description = c("Eluting compounds were detected ...",
                            "Extraction using 80\% methanol"),
            parameters = list(c(paste0("[MS, MS:1000008, ionization type, ",
                              "[MS,MS:1000073, electrospray ionization, ]]"),
                              "param1.2"),
                              paste0("[MSIO, MSIO:0000107, quenching, ",
                                 "[MSIO, MSIO:0000109, liquid nitrogen,]]")),
            replace = TRUE)

getMtdProtocol(mtd)

}
\author{
Gabriele Tomè
}
