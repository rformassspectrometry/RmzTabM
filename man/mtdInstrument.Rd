% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTD.R
\name{mtdInstrument}
\alias{mtdInstrument}
\title{Add or Update Instrument Metadata in an MTD Object}
\usage{
mtdInstrument(
  x = matrix(),
  name = character(),
  source = character(),
  analyzed = character(),
  detector = character(),
  add = TRUE
)
}
\arguments{
\item{x}{A MTD object that stores metadata fields. Defaults to \code{matrix()}.
If all values are \code{NA}, the function returns \code{x} unchanged.}

\item{name}{\code{character} with the name of the instrument used in the
experiment. (e.g., \code{"[MS, MS:1000449, LTQ Orbitrap,]"}).}

\item{source}{\code{character} with the instrument’s source used in the
experiment. (e.g., \code{"[MS, MS:1000073, ESI,]"}).}

\item{analyzed}{\code{character} with the instrument’s analyzer type(s) used in
the experiment.
(e.g., \code{c("analyzer[1]" = "[MS, MS:1000291, linear ion trap,]")}).}

\item{detector}{\code{character} with the instrument’s detector type used in the
experiment. (e.g., \code{"[MS, MS:1000253, electron multiplier,]"}).}

\item{add}{\code{logical} flag controlling how pre-existing instrument
metadata is handled:
\itemize{
\item \code{TRUE} (default): new values are appended to any existing
values.
\item \code{FALSE}: existing instrument metadata is discarded and
replaced entirely by the supplied arguments.
}}
}
\value{
The input object \code{x} updated to include the new or merged instrument
metadata fields. If \code{x} is empty, the empty \code{x}.
}
\description{
Sets or updates instrument-related metadata fields within an MTD (metadata)
in a MTD object. When instrument metadata already exists, the function
can either replace it entirely or append new values to the existing ones.
}
\examples{

x <- mtd_skeleton("001", software = "[MS, MS:1001582, xmcs, 4.0.0]")
## Add instrument metadata to an existing mzTab object
mtd <- mtdInstrument(x, name = "[MS, MS:1000449, LTQ Orbitrap,]",
          source = "[MS, MS:1000073, ESI,]",
          analyzed = c(`analyzer[1]` = "[MS, MS:1000291, linear ion trap,]"),
          detector = "[MS, MS:1000253, electron multiplier,]")

## Replace all existing instrument metadata
mtd <- mtdInstrument(mtd, name = "[MS, MS:1000449, LTQ Orbitrap,]",
          source = "[MS, MS:1000073, ESI,]",
          analyzed = c(`analyzer[1]` = "[MS, MS:1000291, linear ion trap,]"),
          detector = "[MS, MS:1000253, electron multiplier,]",
          add = FALSE)

}
\author{
Gabriele Tomè
}
