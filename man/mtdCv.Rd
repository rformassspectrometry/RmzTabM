% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTD.R
\name{mtdCv}
\alias{mtdCv}
\title{Add or Update Controlled Vocabularies (CV) Metadata in an MTD Object}
\usage{
mtdCv(
  x = matrix(),
  label = character(),
  full_name = character(),
  version = character(),
  uri = character(),
  add = TRUE
)
}
\arguments{
\item{x}{A MTD object that stores metadata fields. Defaults to \code{matrix()}.
If all values are \code{NA}, the function returns \code{x} unchanged.}

\item{label}{\code{character} describing the labels of the controlled
vocabularies/ontologies used in the \emph{mzTab-M} file (e.g. \code{"MS"} for
PSI-MS).}

\item{full_name}{\code{character} describing the full names of the controlled
vocabularies/ontologies used.}

\item{version}{\code{character} describing the version of the controlled
vocabularies/ontologies used. (e.g. \code{"4.1.11"})}

\item{uri}{\code{character} containing the URIs of the controlled vocabularies/
ontologies used in the mzTab file. Note: For OBO ontologies, always use
an OBO PURL rather than raw repository links to ensure long-term
stability (e.g. \code{"https://purl.obolibrary.org/obo/ms.obo"}). For other
ontology formats, please use the fully qualified PURL pointing to the
ontology file.}

\item{add}{\code{logical} flag controlling how pre-existing CV
metadata is handled:
\itemize{
\item \code{TRUE} (default): new values are appended to any existing
values.
\item \code{FALSE}: existing CV metadata is discarded and
replaced entirely by the supplied arguments.
}}
}
\value{
The input object \code{x} updated to include the new or merged CV
metadata fields. If \code{x} is empty, the empty \code{x}.
}
\description{
Sets or updates CV-related metadata fields within an MTD (metadata)
in a MTD object. When CV metadata already exists, the function
can either replace it entirely or append new values to the existing ones.
}
\examples{

x <- mtd_skeleton("001", software = "[MS, MS:1001582, xmcs, 4.0.0]")
## Add CV metadata to an existing mzTab object
mtd <- mtdCv(x, label = "MS",
          full_name = "PSI-MS controlled vocabulary",
          version = "4.1.11",
          uri = "https://purl.obolibrary.org/obo/ms.obo")

## Replace all existing CV metadata
mtd <- mtdCv(mtd, label = "MS",
          full_name = "PSI-MS controlled vocabulary",
          version = "4.1.11",
          uri = "https://purl.obolibrary.org/obo/ms.obo",
          add = FALSE)

}
\author{
Gabriele Tomè
}
