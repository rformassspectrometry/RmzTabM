% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTD.R
\name{mtdContact}
\alias{mtdContact}
\title{Add or Update contact Metadata in an MTD Object}
\usage{
mtdContact(
  x = matrix(),
  name = character(),
  affiliation = character(),
  email = character(),
  add = TRUE
)
}
\arguments{
\item{x}{A MTD object that stores metadata fields. Defaults to \code{matrix()}.
If all values are \code{NA}, the function returns \code{x} unchanged.}

\item{name}{\code{character} contact’s name.}

\item{affiliation}{\code{character} contact’s affiliation.}

\item{email}{\code{character} contact’s e-mail address.}

\item{add}{\code{logical} flag controlling how pre-existing contact
metadata is handled:
\itemize{
\item \code{TRUE} (default): new values are appended to any existing
values.
\item \code{FALSE}: existing contact metadata is discarded and
replaced entirely by the supplied arguments.
}}
}
\value{
The input object \code{x} updated to include the new or merged contact
metadata fields. If \code{x} is empty, the empty \code{x}.
}
\description{
Sets or updates contact-related metadata fields within an MTD (metadata)
in a MTD object. When contact metadata already exists, the function
can either replace it entirely or append new values to the existing ones.
}
\examples{

x <- mtd_skeleton("001", software = "[MS, MS:1001582, xmcs, 4.0.0]")
## Add contact metadata to an existing mzTab object
mtd <- mtdContact(x, name = "Name Surname",
          affiliation = "PSI-MS",
          email = "name.surname@mail.com")

## Replace all existing contact metadata
mtd <- mtdContact(mtd, name = "Name Surname",
          affiliation = "PSI-MS",
          email = "name.surname@mail.com",
          add = FALSE)

}
\author{
Gabriele Tomè
}
