% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTD.R
\name{setMtdCv}
\alias{setMtdCv}
\alias{setMtdCv,dfmatrix-method}
\alias{setMtdCv,MzTabM-method}
\alias{getMtdCv}
\title{Add or Update Controlled Vocabularies (CV) Metadata  of an mzTab-M
MTD section}
\usage{
\S4method{setMtdCv}{dfmatrix}(
  x = matrix(),
  label = character(),
  full_name = character(),
  version = character(),
  uri = character(),
  replace = FALSE
)

getMtdCv(x = matrix())
}
\arguments{
\item{x}{A MTD section that stores metadata fields. Can be a two-column
\code{character} matrix, a two-column \code{data.frame} or a \code{\link[=MzTabM]{MzTabM()}} object.
Defaults to \code{matrix()}. If all values are \code{NA}, the function returns \code{x}
unchanged.}

\item{label}{\code{character} describing the labels of the controlled
vocabularies/ontologies used in the \emph{mzTab-M} file (e.g. \code{"MS"} for
PSI-MS).}

\item{full_name}{\code{character} describing the full names of the controlled
vocabularies/ontologies used.}

\item{version}{\code{character} describing the version of the controlled
vocabularies/ontologies used. (e.g. \code{"4.1.11"})}

\item{uri}{\code{character} containing the URIs of the controlled vocabularies/
ontologies used in the mzTab file. Note: For OBO ontologies, always use
an OBO PURL rather than raw repository links to ensure long-term
stability (e.g. \code{"https://purl.obolibrary.org/obo/ms.obo"}). For other
ontology formats, please use the fully qualified PURL pointing to the
ontology file.}

\item{replace}{\code{logical} flag controlling how pre-existing CV
metadata is handled:
\itemize{
\item \code{FALSE} (default): new values are appended to any existing
values.
\item \code{TRUE}: existing instrument metadata is discarded and
replaced entirely by the supplied arguments.
}}
}
\value{
\itemize{
\item For \code{setMtdCv()}: the input object \code{x} updated to include the new or
merged CV metadata fields. If \code{x} is empty, the empty \code{x}.
\item For \code{getMtdCv()}: a named \code{character} with the CV information, names being
the field names.
}
}
\description{
\code{setMtdCv()} sets or updates CV-related metadata fields within an MTD
(metadata) section. When CV metadata already exists, the function can either
replace it entirely or append new values to the existing ones.

\code{getMtdCv()} returns the CV information from an MTD section.
}
\examples{

x <- mtdSkeleton("001", software = "[MS, MS:1001582, xmcs, 4.0.0]")
## Add CV metadata to an existing mzTab object
mtd <- setMtdCv(x, label = "MS",
          full_name = "PSI-MS controlled vocabulary",
          version = "4.1.11",
          uri = "https://purl.obolibrary.org/obo/ms.obo")

## Replace all existing CV metadata
mtd <- setMtdCv(mtd, label = "MS",
          full_name = "PSI-MS controlled vocabulary",
          version = "4.1.11",
          uri = "https://purl.obolibrary.org/obo/ms.obo",
          replace = TRUE)

## Get CV infrmation
getMtdCv(mtd)

}
\author{
Gabriele Tomè
}
