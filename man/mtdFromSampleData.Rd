% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTD.R
\name{mtdFromSampleData}
\alias{mtdFromSampleData}
\alias{sampleCols}
\alias{msRunCols}
\alias{assayCols}
\title{Create the mzTab-M MTD content from a sample data frame}
\usage{
mtdFromSampleData(
  x,
  sampleCols. = sampleCols(),
  msRunCols. = msRunCols(),
  assayCols. = assayCols(),
  groups = character(),
  group_description = character(),
  group_type = character(),
  group_datatype = character(),
  group_unit = character(),
  average_function = "[MS, MS:1002962, mean, ]",
  variation_function = "[MS, MS:1002963, variation coefficient, ]",
  description = character()
)

sampleCols(
  sample = "sample",
  species = "species",
  tissue = "tissue",
  cell_type = "cell_type",
  disease = "disease",
  description = "description",
  ...
)

msRunCols(
  location = "location",
  instrument_ref = "instrument_ref",
  format = "format",
  id_format = "id_format",
  fragmentation_method = "fragmentation_method",
  scan_polarity = "scan_polarity",
  hash = "hash",
  hash_method = "hash_method"
)

assayCols(
  assay = "assay",
  external_uri = "external_uri",
  sample_ref = "sample_ref",
  ms_run_ref = "ms_run_ref",
  ...
)
}
\arguments{
\item{x}{\code{data.frame} with information on samples, MS runs and assays. Each
row is expected to represent one MS run (MS data file) and columns
providing information on the measured sample(s) along with
experimental and technical information.}

\item{sampleCols.}{named \code{character} vector defining the columns in \code{x}
containing information for the individual sample fields (with names
being the name of the mzTab-M field and values the respective column
name in \code{x}). The \code{sampleCols()} function can be used to define this
parameter. See examples below and \code{\link[=mtdSample]{mtdSample()}} for more information.}

\item{msRunCols.}{named \code{character} vector defining the columns in \code{x}
containing information on the MS runs (with names being the name of the
mzTab-M field and values the respective column name in \code{x}). Required
fields/parameters are \code{location} and \code{scan_polarity}. The \code{msRunCols()}
function can be used to define this parameter. See examples below and
\code{\link[=mtdMsRun]{mtdMsRun()}} for more information.}

\item{assayCols.}{named \code{character} vector defining the columns in \code{x}
containing information for the individual assays fields (with names
being the name of the mzTab-M field and values the respective column
name in \code{x}). Parameter/field \code{assay} is required. The \code{assayCols()}
function can be used to define this parameter. See examples below and
\code{\link[=mtdAssay]{mtdAssay()}} for more information.}

\item{groups}{\code{character} with the names of the columns in \code{x} that should
be considered as \emph{study variable groups}. If not defined (the default)
a single study variable group \code{"undefined"} and single study variable
\code{"undefined"} will be used.}

\item{group_description}{\code{character} with an optional description of each
study variable group. If provided its length has to match the length
of parameter \code{groups}.}

\item{group_type}{\code{character} defining the type for each study variable
group. If provided its length has to match the length of parameter
\code{groups}. Supported values are
\code{"[STATO, STATO:0000252, categorical variable, ]"},
\code{"[STATO, STATO:0000228, ordinal variable, ]"} and
\code{"[STATO, STATO:0000251, continuous variable, ]"} for categorical,
ordinal or numerical values, respectively. If not provided (the default)
the study variable group type will be inferred from the data type of the
respective columns in \code{x}.}

\item{group_datatype}{optional \code{character} defining the data type of the
values (i.e., study variables) for the study variable group. If provided,
its length has to match the length of paramter \code{groups}.
Supported values are \code{"xsd:string"}, \code{"xsd:integer"}, \code{"xsd:decimal"},
\code{"xsd:boolean"}, \code{"xsd:date"}, \code{"xsd:time"}, \code{"xsd:dateTime"},
\code{"xsd:anyURI"}, and \code{"Parameter"} (for \emph{CV Parameters}). Date, time and
dateTime values \strong{must} be encoded in ISO 8601 format. If not provided
the type is guessed by the data type of the respective column in \code{x}.}

\item{group_unit}{optional \code{character} defining the unit of the group
variable (for numeric data types). If provided, its length has to match
the length of parameter \code{groups}. \code{NA} or \code{""} has to be provided for
groups for which no unit should be reported. By default
(\code{group_unit = character()}) no unit is reported for any group.}

\item{average_function}{optional \code{character} defining the function used to
calculate the study variable quantification value (reported in the
following table(s)). Can be of length 1 or equal to the number of study
variables (to allow defining a different function per variable). Use
\code{mtdDefineStudyVariables()} to get the complete set of study
variables for parameters \code{x} and \code{groups}. Defaults to the arithmetic
mean (\code{average_function = "[MS, MS:1002962, mean, ]"}).}

\item{variation_function}{optional \code{character} defining the function used to
calculate the study variable quantification variation value (reported in
the following table(s)). Can be of length 1 or equal to the number of
study variables (to allow defining a different function per variable).
Use \code{mtdDefineStudyVariables()} to get the complete set of study
variables for parameters \code{x} and \code{groups}. Defaults
to the coefficient of variation
(\code{variation_function = "[MS, MS:1002963, variation coefficient, ]"}).}

\item{description}{\code{character} of length equal to \code{length(sample)} with
optional description of each sample.}

\item{sample}{\code{character} with the labels/names of the individual samples.}

\item{species}{\code{list} of length equal to \code{length(sample)} with each element
providing the species (eventually multiple) for each sample. Can also
be a \code{character} of \code{length(sample)} to assing a single species to each
\code{sample}, or a \code{character(1)} of length one to assign the same species
to every sample.}

\item{tissue}{\code{list} with the tissue(s) of each sample. The same format
as described for parameter \code{species} can be used.}

\item{cell_type}{\code{list} with the cell type(s) of each sample. The same format
as described for parameter \code{species} can be used.}

\item{disease}{\code{list} with the disease(s) of each sample. The same format
as described for parameter \code{species} can be used.}

\item{...}{named \code{character} vectors of length equal to the length of
parameter \code{sample} with optional \emph{custom} information for each
individual sample.}

\item{location}{\code{character} with the location (and file name) of the
individual runs. Each element will be one run. This parameter is
required, set to \code{"null"} if the location of the file(s) is not known.}

\item{instrument_ref}{(optional) \code{integer()} with the index of the instrument
the run was measured on.}

\item{format}{(optional) \code{character} defining the format of the external MS
data file. If specified, also \code{id_format} has be be provided. Can be of
length 1 or equal to \code{length(location)}. For data file(s) in mzML format,
\code{format = "[MS, MS:1000584, mzML file, ]"} can be used.}

\item{id_format}{(optional) \code{character} defining the id format used in the
external data file. If specified, also \code{format} needs to be defined.
Can be of length 1 or equal to \code{length(location)}. For data file(s) in
mzML format, \code{format = "[MS, MS:1000530, mzML unique identifier, ]"}
can be used.}

\item{fragmentation_method}{(optional) \code{list} of \code{character} defining the
type(s) of fragmentation(s) used in a given ms run. Length must match
length of \code{location} if provided. If no fragmentation was used for a
specific file/run use \code{NULL} for that \code{list} element (position). As
example, if two runs are included, the first does not have any
fragmentation and for the second CID and HCD was used define
\code{list(NULL, c("[MS, MS:1000133, CID, ]", "[MS, MS:1000422, HCD, ]"))}.}

\item{scan_polarity}{\code{character} defining the polarity of a run. Can be
either \code{"positive"} or \code{"negative"}. Can be of length 1 or equal to
\code{length(location)}.}

\item{hash}{(optional) \code{character} with the hash value of the corresponding
external MS data file. If provided, also \code{hash_method} needs to be
defined. The length of \code{hash} has to match the length of \code{location}.}

\item{hash_method}{(optional) \code{character} with the hash method used to
generate the value in \code{hash}. If provided, also \code{hash} needs to be
defined. The length of \code{hash_method} has to match the length of \code{hash}.}

\item{assay}{\code{character} with the names of the assay(s). Each assay \strong{must}
be reported in the following sections (e.g. the SMF section).}

\item{external_uri}{optional \code{character} with a reference to further
information about the assay, for example via a reference to an object
within an ISA-TAB file. Can be of length 1 (in which case the same
reference is assigned to every assay) or length equal to the length
of \code{assay}.}

\item{sample_ref}{optional \code{character} with the ID/name of the sample for
the assay (e.g. \code{"sample[1]"}). If provided, its length has to match
the length of \code{assay}.}

\item{ms_run_ref}{\code{character} with the ID of associated \emph{ms_run}(s). For
multiplexed assays, different assays can refer to the same run. To
support pre-fractionated samples, it is also possible to provide a
\code{list} of \code{character} with the runs the assay was measured in. See
examples below for more details.}
}
\value{
two column \code{character} \code{matrix}.
}
\description{
\code{mtdFromSampleData()} compiles the \emph{sample}, \emph{ms_run}, \emph{assay} and
\emph{study variable} content of the MTD section from a \emph{sample data}
\code{data.frame}. Each row of this \code{data.frame} (parameter \code{x}) is expected to
represent on MS run (i.e., acquisition of a sample) of an experimen with
columns containing information on the individual MS run and the sample(s).

The columns providing information on the MS run can be specified with
parameter \code{msRunCols}. Mandatory columns are \emph{location} and \emph{scan_polarity}.

The columns providing information on the individual \emph{assays} can be defined
with parameter \code{assayCols}. In most cases one assay represents one MS run.

The columns from which sample information can be retrieved can be defined
with \code{sampleCols}. See notes below for more information.

The columns containing experimental/phenotype information can be defined
with parameter \code{groups}.

Arguments defining columns for MS runs, assays and samples are grouped with
parameters \code{msRunCols.}, \code{assayCols.} and \verb{sampleCols,}. For each a helper
function is available providing defaults and assisting in defining the
arguments. See section \emph{Defining columns with information on MS runs, assays
and samples}

The MTD information is compiled from the sample data \code{x} as follows:
\itemize{
\item MS runs: each row in \code{x} is added as one MS run. The column in \code{x} with
the respective file name needs to be defined through parameter
\code{msRunCols.}.
\item samples: the unique set of rows based on the column(s) defined with
\code{sampleCols.} are added as samples.
\item assay: each unique element in the column defined through \code{assayCols.}
(\code{assay}) is added as one \emph{assay} referencing to samples and MS runs.
In most cases the number of MS runs will match the number of assays, i.e.,
each row in \code{x} is one MS run as well as one assay. A 1:n mapping between
assay and MS run is also possible.
\item study variables: each column defined with parameter \code{groups} is added as
one \emph{study variable group}.
}
}
\note{
The SML and SMF sections must report one column for each \strong{assay}. Thus,
each row in the input sample data \code{x} is expected to be one assay.

In mzTab-M a \emph{sample} is the source of an biological sample. If in an
experiment e.g. multiple blood samples are taken at different time points
from the same individual, the individual is considered a single sample.
Information on the time points can be provided as study variables using the
\code{groups} parameter.
}
\section{Defining columns with information on MS runs, assays and samples}{

\itemize{
\item \code{msRunCols()}: function to create a named \code{character} defining the columns
in \code{x} containinig information for the individual parameters. To change
the default for the column containing the MS data file names (default
\code{location = "location"}) to a column called e.g. \code{"mzml_file"}:
\code{msRunCols(location = "mzml_file")}. Parameters \code{location} and
\code{scan_polarity} have to be set to match the column names in \code{x} with the
respective information. See examples for more information.
\item \code{assayCols()}: function to define columns containing information on assays.
Parameter \code{assay} has to be adapted. Through \code{...} it is also possible
to define additional columns with optional information on each assay.
\item \code{sampleCols()}: function to define columns containing sample information.
Through \code{...} additional optional columns with sample information can be
provided.
}
}

\examples{

## Defining an example sample data:
## - file: the mzML file, i.e., the *MS run*.
## - name: the name of the measurement. This is also the name of the sample:
##         QC is the pool of all samples, s1 to s4 the ID of the individual.
## - phenotype: defining the biological replicates, 2 for CVD, 2 for CTR.
## - age: covariate, age of the individuals.
## - injection_index: the order in which samples were measured.
sdata <- data.frame(
    file = c("1.mzML", "2.mzML", "3.mzML", "4.mzML", "5.mzML", "6.mzML"),
    name = c("QC", "s1", "s2", "QC", "s3", "s4"),
    phenotype = c(NA, "CVD", "CTR", NA, "CTR", "CVD"),
    age = c(NA, 35, 32, NA, 43, 32),
    injection_index = c(1, 2, 3, 4, 5, 6))
## Add additional required columns:
sdata$polarity <- "positive"

## Add columns with optional, additional information to the individual
## samples or assays.
sdata$organism <- "[NCBITaxon, NCBITaxon:9606, Homo sapiens, ]"
sdata$assay_info <- c("run1", "run2", "run3", "run4", "run5", "run6")

## Define the columns in `sdata` that provide information on the individual
## samples.
scols <- sampleCols(sample = "name", species = "organism")

## Define the columns in `sdata` that provide MS run information
mscols <- msRunCols(location = "file", scan_polarity = "polarity")

## Define the columns in `sdata` that provide assay information; we use
## the MS run/file name also for the assay name and add an additional
## column with optional content/information.
acols <- assayCols(assay = "file", assay_info = "assay_info")

## Create the MTD section from the `sdata` `data.frame`. Parameter `groups`
## allows to define the columns in `sdata` that should be encoded as
## *study variable groups*.
m <- mtdFromSampleData(sdata, sampleCols = scols, msRunCols = mscols,
    assayCols = acols, groups = c("phenotype", "age", "injection_index"))

## The sample to assay mapping:
## The repeated injection (assay 1 and 4) of the QC sample are assigned
## to the same sample (1).
getMtdField(m, "assay\\\\[\\\\d\\\\]-sample_ref")

## The study variable 2 represents a value of `"CVD"` for the *phenotype*
## study variable group
getMtdField(m, "study_variable\\\\[2\\\\]$")

## and this study variable references the 2nd and 6th assay
getMtdField(m, "study_variable\\\\[2\\\\]-assay_refs")


## It is also possible to create a MTD section without samples or
## study variables
m <- mtdFromSampleData(
    sdata,
    msRunCols = c(location = "file", scan_polarity = "polarity"),
    assayCols = c(assay = "assay_info"))
m
}
\seealso{
\code{\link[=mtdToSampleData]{mtdToSampleData()}} for the inverse function.
}
\author{
Johannes Rainer
}
